% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{form}
\alias{form}
\title{Form}
\usage{
form(..., method = "get")
}
\arguments{
\item{...}{tag attributes and children passed to the \code{htmltools::tags$form()} function}

\item{method}{character scalar denoting the HTTP
method to perform.
One of:
\itemize{
\item "get"
\item "post"
\item "put"
\item "patch"
\item "delete"
}}
}
\value{
(When injected) An \code{htmltools::tag} object.
}
\description{
When used within a \code{\link[=template]{template()}}, a form implementation is injected that
wraps \code{htmltools::tags$form()}. This provides additional functionalities
when a request context is available.
}
\details{
Do not call the exported function directly, it is a stub.
\subsection{CSRF}{
\itemize{
\item If CSRF middleware is active, a hidden \code{csrf_token} input is
automatically injected.
}
}

\subsection{Method Spoofing}{

If \code{method} is one of "put", "patch", or "delete", a hidden \verb{_method}
input is added and the HTML form method is set to "post".

Browsers only support GET and POST.
When method is "put", "patch", or "delete", freshwater renders a POST form
with a hidden _method field. Middleware interprets this as the
effective HTTP method. Requires freshwater context-enabled middleware.
}
}
\examples{
page <- template({
     form(method = "delete")
})
page()
}
\seealso{
\link{template}, \link{api_csrf}, \link{api_freshwater}, \link[htmltools:builder]{htmltools::tags}
}
