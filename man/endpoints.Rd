% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url.R
\name{endpoints}
\alias{endpoints}
\title{Reverse Routing}
\usage{
endpoints(route = NULL, api = NULL, refresh = FALSE)
}
\arguments{
\item{route}{the route group to retrieve endpoints from; typically defined either via the file name, routeName or route in plumber2. If NULL, will return all endpoints for all routes.}

\item{api}{a \link[plumber2:plumber2-package]{plumber2::plumber2} api object. If NULL, context is used to find the api.}

\item{refresh}{force refresh the registered routes. Useful if you have added routes after calling \code{endpoints()}.}
}
\value{
If \code{route} is \code{NULL}, returns a list of route groups
and their endpoints. Otherwise returns a list of a
route's endpoint accessors.
}
\description{
Access generated endpoint URL helpers.
}
\details{
Alias rules:
\itemize{
\item "/" endpoints become "index"
\item GET endpoints are accessed directly, like index()
\item non-GET endpoints require an accessor, like index$delete()
\item path parameters are removed from the alias and used to disambiguate overloaded helpers via named
function args
\item Reserved argument: \code{.query} argument constructs a query from a named list
\item Reserved argument: \code{.anchor} constructs an anchor from a character scalar
}

For example:
\itemize{
\item \verb{GET /} -> \code{index()}
\item \verb{POST /} -> \code{index$post()}
\item \code{GET /my/filter} -> \code{my_filter()}
\item \verb{GET /users/:id} -> \code{users(id = 1, .query = list(page = 2))}
\item \verb{GET /users/:id} -> \code{users(id = 1, .anchor = "details")}
\item \verb{GET /users/:id} -> \code{users(id = 1, .query = list(page = 2), .anchor = "details")}
\item \verb{DELETE /users/:id} -> \code{users$delete(id = 1)}
\item \verb{DELETE /users/:name} -> \code{users$delete(name = "Jim")}
}

Ambiguous endpoint shapes will result in an error. It is recommended to ensure endpoints
have different shapes. For example, the following cannot be disambiguated by \code{endpoints()}
and thus results in an error:
\itemize{
\item \verb{GET /foo/:id/bar} -> \verb{foo_bar(id = _)}
\item \verb{GET /foo/bar/:id} -> \verb{foo_bar(id = _)}
}
}
\examples{
#* @plumber
function(api) {
   api |>
     api_freshwater()
}

#* @get /
#* @serializer html
#* @routeName user
function() {
     endpoints("user")$index()
}
}
